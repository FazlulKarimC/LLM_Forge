{
    "name": "llmforge-backend",
    "private": true,
    "scripts": {
        "dev": "set PYTHONIOENCODING=utf-8 && venv\\Scripts\\uvicorn.exe app.main:app --reload --reload-dir app --port 8000"
    }
}
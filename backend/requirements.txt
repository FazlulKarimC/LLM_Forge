# ============================================================
# LlmForge Backend - Dependencies
# ============================================================

# --- Core ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0

# --- Inference (HuggingFace API - lightweight, no PyTorch) ---
huggingface-hub>=0.20.0

# --- Metrics Computation ---
numpy>=1.24.0
statsmodels>=0.14.0

# --- Utilities ---
python-dotenv>=1.0.0
httpx>=0.26.0
tenacity>=8.2.0

# --- Background Tasks (RQ + Upstash Redis) ---
rq>=1.16.0
redis>=5.0.0

# --- RAG Pipeline (lightweight — all models via HF API) ---
qdrant-client>=1.7.0
rank-bm25>=0.2.2
